spring:
  application:
    name: track-calories
  datasource:
    url: jdbc:postgresql://localhost:5432/calories_db
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
